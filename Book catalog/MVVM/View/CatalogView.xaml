<UserControl x:Class="Book_catalog.MVVM.View.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Book_catalog.MVVM.View"
             xmlns:viewModel="clr-namespace:Book_catalog.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="720">
    
    <UserControl.DataContext>
        <viewModel:CatalogViewModel />
    </UserControl.DataContext>
    
    <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0.35*"/>
            </Grid.ColumnDefinitions>
            
            <DataGrid Margin="5"
                      AutoGenerateColumns="False"
                      AlternatingRowBackground="#B5BFC6"
                      Background="#E4EBF1">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Book Details">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="C:\\Users\\Asus\RiderProjects\\Book catalog\\Book catalog\\Icons\BookIcon.png" Width="24" Height="24"/>
                                    <TextBlock Text="Book name" Margin="5 0 0 0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            
            <StackPanel Grid.Column="1">
                
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
